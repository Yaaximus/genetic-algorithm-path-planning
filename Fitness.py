class Fitness(object):

    def __init__(self, path_points, chr_len, new_pop, pop_max):

        self._path_points = path_points
        self._chr_len = chr_len
        self._new_pop = new_pop
        self._pop_max = pop_max

    def calculate_fitness(self):

        pass